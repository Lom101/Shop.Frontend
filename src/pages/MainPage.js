import React from 'react';
import MainCarousel from '../components/MainCarousel';

const MainPage = () => {
    return (
        <div>
            <MainCarousel />

        </div>
    );
};

export default MainPage;